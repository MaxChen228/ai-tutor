# --- Core Framework ---
# Web應用框架與WSGI伺服器
Flask>=2.3.0
gunicorn>=21.0.0
Flask-Cors>=4.0.0

# --- Database ---
# PostgreSQL資料庫驅動 (推薦使用psycopg v3，解決Python 3.11+的相容性問題)
psycopg[binary]>=3.1.0

# --- AI / Machine Learning ---
# 大型語言模型 (LLM) 功能 (解決 openai/gemini 未安裝的警告)
openai>=1.12.0
google-generativeai>=0.4.0

# 語義向量模型 (解決 huggingface_hub 的導入錯誤)
sentence-transformers>=2.7.0
huggingface-hub>=0.20.0
transformers>=4.38.0
torch>=2.2.0
torchvision>=0.17.0

# --- Authentication ---
# 使用者認證與JWT權杖管理
Flask-JWT-Extended>=4.5.0

# --- Utilities ---
# 環境變數管理
python-dotenv>=1.0.0

# 科學計算與數據處理，通常是AI/ML函式庫的底層依賴
numpy>=1.26.0
scikit-learn>=1.4.0